// Leetcode 9
// Easy

class Solution {
    public boolean isPalindrome(int x) {
        if(x<0)return false;
        int y=x;
        int out, rev=0;
        while(y!=0){
            rev=(rev*10)+(y%10);
            y=y/10;
        }
        if(rev==x)return true;
        return false;
    }
}
